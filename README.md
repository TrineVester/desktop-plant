# Desktop Plant

A transparent desktop plant pet inspired by the *don’t touch me* plant.

## Features
- Transparent, always-on-top window
- Daily growth logic with watering + fertilizer
- Hover to close the leaves

## Run
1. Install dependencies: `npm install`
2. Start the app: `npm run start`

## Build (Windows)
`npm run build`
